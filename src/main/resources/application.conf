my-app {
  routes {
    # If ask takes more time than this to complete the request is failed
    ask-timeout = 5s
  }
}
akka.http.server.remote-address-header = on # For getting user IP client side
threshold = 5